# entr Source

Source snapshot:

- GitHub: `https://github.com/eradman/entr.git`
- Commit: `8e2e8b4`
- Local snapshot: `github/`

The nested `.git` directory was intentionally removed after checkout so the
outer repository can version this as a plain source snapshot.
