# I/O behavior tests

These tests validate the observable command-line I/O behavior of `./executable` (entr 5.7): help/usage output, stdin-driven file list handling, placeholder substitution (`/_`), and exit codes in non-interactive one-shot mode (`-n -z`).

Run:

```bash
./eval/run.sh
```
