## I/O behavior tests

These tests validate the observable CLI I/O behavior of `./executable` (bat) via a local in-process HTTP server.

Run:

```bash
./eval/run.sh
```

Results are written to `eval/results.xml` (JUnit XML).
